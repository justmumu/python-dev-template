# mdformat config — markdown formatter (single source of truth for *.md)
number = true      # keep consecutive ordered-list numbering (1. 2. 3.)
wrap = "keep"      # don't rewrap prose
end_of_line = "lf"
